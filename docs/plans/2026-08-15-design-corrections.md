# Corrections §22 forces on the rest of the document

> **APPLIED, 2026-08-16.** Every row below is either done or marked `CLOSED`. The record stays because the
> rows carry the MEASUREMENT that forced each change, which the corrected text does not always repeat, and
> because three rows were applied differently from how they were written — each says so in place. Verified
> by re-searching every quoted fragment in its target: the refuted census, the null-`pid` claim, the
> two-rung tile ruling, the `2.1.232` version, the `agents.go:94` and `registry.go:189` citations, the
> dead `logs` refusal text and the `working/active` pair notation are all absent outside §22, which carries
> them deliberately as the figures it withdraws.
>
> The audit that closed this list also found three sites the list itself had missed, because a first pass
> checked only the first 60 characters of each quoted cell and every "keep X and add Y" row matched itself:
> §21.3's own restatement of the tile ruling, §17's `--all` justification, and the `agents.go:127` citation
> with its `6 to 25` figure. **A corrections list needs a completion check that compares the WHOLE quoted
> fragment, not its head** — otherwise the rows that change a sentence's tail report as done while standing.

**What this is.** `docs/design.md` §22 landed on 2026-08-15 measured against both versions this fleet
runs. Re-measuring refuted figures the document had already published, in some cases at six sites each.
This is the work list for correcting them: one row per site, with the text as it stands now, what it must
say, and the measurement that forces the change. It is a plan rather than a note because applying it
touches three documents and four Go files, and because several rows are judgement calls that must be read
before they are applied.

**How it was verified, and why that had to be done twice.** Every line number was first opened and read at
commit `a4a08e2`, and a gate walked all 88 `file:line` anchors plus 51 bare `:NNN` references to confirm the
quoted fragment sits on the cited line or within one of it. That was not enough: two commits after `a4a08e2`
moved lines this list points at, so four anchors were stale the moment it was committed — the pid paragraph
and the version table's rows had swapped places, and one row's target had been deleted outright. **A
corrections list is invalidated by any edit to the file it targets**, so it is re-anchored after every commit
that touches one, by searching for the QUOTED TEXT rather than by shifting the number. Re-anchored and
re-checked at the current HEAD: 50 anchors, 0 misses, and rows whose work is already done say `CLOSED` and
name the commit that did it rather than being deleted.

**One host alias is withheld.** `docs/` is served publicly, so the third machine in §17's table is named
here only by its position. §17 itself publishes the alias; this file does not add a second place that does.

| file:line | what it says now (quoted) | what it must say | the measurement that forces it |
|---|---|---|---|
| docs/design.md:25 | "of 21 real rows, 10 were `background` and every waiting row was among them" | "Measured 2026-08-15 with `claude agents --json --all` on two hosts: 45 rows, 32 `background` (local 31/25, `nuc` 14/7). The earlier 10-of-21 figure was a bare-`--json` count." Drop "every waiting row was among them" | §22.1: `--all` on 2.1.233 + 2.1.224 gives 45 rows / 32 background = 71%; bare gives local 13, `nuc` 11. The waiting half is UNVERIFIED at the new denominator and carries a §22.10 probe — `block.questions` is on 1 of the 6 rows the listing calls `blocked`, and a Write-permission prompt polls `working/active` for 136 s |
| docs/design.md:731 | "§22.1 puts the scale on that: of 21 real rows, 10 were `background` and **every waiting row was among them**" | same replacement as :25, citing §22.1 for the call and the date | as above |
| docs/design.md:1576 | "§22 changes the population this question is about: of 21 real rows 10 are `background` and **every waiting row**…" | same replacement; §14.2's clause survives unchanged (§22.5 rules the out-of-band signal producer-side or nothing) | as above |
| docs/design.md:2149 | "**This key cannot name a row with no pane, and §22 makes those rows the majority** — 10 of 21, every waiting one" | "…the majority — 32 of 45 under `--all`, 2026-08-15 (§22.1)"; and §22.5 now RULES `x` on a pane-less row a refusal rather than leaving the key open | as above, plus §22.5: `hide.KeyOf` has no `Kind` branch, so an agent row's key is `{Host, name, 0, 0, ""}` and two same-named rows on one host share it |
| docs/design.md:2657 | "**10 of 21 rows and 5 of 5 waiting rows have no pane at all**" | "32 of 45 rows have no pane at all (2026-08-15, `--all`, two hosts, §22.1)". The waiting clause goes | as above; §21.1's argument (the agent cwd must be fetched) is unaffected and gets stronger |
| docs/design.md:2655 | "`agents.Session.CWD` — present in **25 of 25** real `state.json` files" | "present in 25 of 25 (2026-08-14) and in the listing's own nine keys, which include `cwd` on both 2.1.233 and 2.1.224 (§22.6)" | §22.6: the listing schema is nine keys, `cwd` among them, identical on both versions — a stronger source than the file |
| internal/registry/registry.go:273-274 | "Unassigned, which is the bucket the project list pins LAST: 10 of 21 rows and 5 of 5 WAITING rows on the measured fleet" | "…: 32 of 45 rows under `--all` on 2026-08-15 (docs/design.md §22.1)". Drop the waiting clause | as :25. A production comment carrying a refuted census is the shape that stops the next reader checking |
| internal/registry/agentkey_test.go:93 | "An agent row's project comes from Claude's own `cwd`, and 10 of 21 rows plus 5 of 5" | same replacement | as :25 |
| internal/registry/waiting_test.go:127 | "An agent row waits too, and 5 of 5 waiting rows on the measured fleet had no pane at" | "…and every waiting row on the 21-row fleet of 2026-08-13 had no pane; the claim is not re-derived at the 45-row denominator (§22.1)" | as :25 — the sentence is a comment on a live test and must not assert what §22.1 withdrew |
| internal/registry/registry.go:246-247 (the sentence wraps: the first clause is on :246, "`(host, sessionId)` was unique for 20" on :247) | "`(host, sessionId, cwd)` was unique for 21 of 21 rows where `(host, sessionId)` was unique for 20" | keep the historical measurement, and add: "under `--all` (§22.6) the same key gives 31 rows / 30 distinct keys / 1 row lost, so `Kind` joins the key (§22.11)" | §22.11: mirroring `agentRowID` over the real `--all` listing gives 13/13/0 bare, 31/30/1 with `--all`, 17/17/0 without `done` |
| internal/registry/agentkey_test.go:21 | the same `(host, sessionId, cwd)` sentence in the test's own comment | the same addition | as the row above; split so each anchor is checkable |
| docs/design.md:1864-1865 (the sentence STARTS at :1864; the reconciliation cited :1865, which holds only its second half) | "over 25 real `state.json` files `needs` is present in **4**, while `detail` is present in **25 of 25**" | "over 32 files (25 local + 7 `nuc`, 2026-08-15) `detail` is present in **31/32** and `needs` in **6/32**; at most one file lacks `detail`, and whether any lacks both is unmeasured" | §22.5's census, 32 files, 0 unparsable |
| docs/design.md:1868 | "So a tile shows `detail`, prefers `needs` where it exists, says which one it is showing, and ages it" | "…and falls back to the listing's own word, then to `unknown`" — the ruling needs the third and fourth rungs | §22.5: `detail` 31/32, and 2 of the 6 local interactive rows carry neither `state` nor `status` (derived from `status` 4/6 and `state` 0/6), so `Attention()`'s default renders `unknown` |
| docs/design.md:2729 | "So the tile shows **`detail`**, with `needs` preferred when present." | the same four-rung ruling as :1868 — otherwise the document carries a two-rung and a four-rung version of one rule | §22.5. This is §21.3's own restatement of the :1868 ruling and was missing from the earlier list |
| docs/design.md:2037 | "plus `detail` on every row (25/25) and a `needs` sentence on some (4/25)" | "`detail` on 31 of 32 and `needs` on 6 of 32 (2026-08-15)" | §22.5's census |
| docs/design.md:2728 | §21.3's table: "`detail` \| **25/25**" | keep the 25-file row, dated, and add the 32-file figure beside it: `detail` 31/32 | §22.5's census supersedes five of §21.3's six rows; the rows below give the other four |
| docs/design.md:2732 | §21.3's table: "`needs` \| **4/25**" | keep, dated, and add `needs` 6/32 | §22.5's census |
| docs/design.md:2723 | §21.3's table: "`respawnFlags` \| 25/25 \| the respawn recipe, a flag list" | keep, dated, and add 32/32 | §22.5's census. The earlier list claimed five superseded rows and instructed only two; this is one of the two it skipped |
| docs/design.md:1860 | §21.3's table: "`resumeSessionId` \| 25/25 \| **separate from `sessionId`** — not always the id you resume with" | keep, dated, and add 32/32. Do **not** add a rate at which the two differ: presence was counted, difference was not (§22.10) | §22.5's census. §22.1's retirement of `--resume` rests on the categorical fact this cell already states, not on a rate |
| docs/design.md:2731 | §21.3's table: "`state` \| 25/25 \| `blocked` on 5 of 25" | "`state` 32/32; the LISTING says `blocked` on 5 rows locally and 6 fleet-wide, and `block.questions` is present on 1 of those 6" | §22.5: file `state` 32/32, `block` 1/32; the file's word and the listing's word disagree on 1 of 25 for a semantic reason |
| docs/design.md:2733 | §21.3's table: "`intent` \| 25/25 \| **the OPERATOR's own prompt**" | leave the figure and label it "not re-measured on 2026-08-15" | §22.5 explicitly did not re-count `intent`; the ruling is about what the field means, so it stands on the earlier census |
| docs/design.md:1840-1842 (the claim wraps across three lines; the reconciliation cited :1842, which holds only its closing `state.json` reference) | "**`--all` is not optional, and the listing's `state` is derived rather than read.** Bare `--json` omitted a reaped row entirely, and the `state` it published disagreed with that row's own `state.json`" | Keep "`--all` is not optional" and replace its justification: bare hid **17 of 31** local rows (13 `done`, 4 `failed`), not one reaped row; and the listing is AUTHORITATIVE for state — the two sources agree on 24 of 25 and the one divergence is semantic, on a file 0.3 minutes old | §22.6 (population) and §22.5 (job `77ef6f5e`: file `blocked`, listing `working`, `tempo: active`, `inFlight {tasks: 5}`, no `block`) |
| docs/design.md:1933 | "the producer still calls bare `claude agents --json` (`internal/agents/agents.go:127`) … on one machine `--all` moved the background count from 6 to 25" | "the producer still calls bare `claude agents --json` at TWO sites, `internal/agents/agents.go:138` (local argv) and `:150` (the ssh shell string) … locally bare returns 13 rows against `--all`'s 31, 17 sessionIds present only under `--all` (13 `done`, 4 `failed`)" | §22.6. `:127` is the `Fetcher` interface doc comment, verified today; a fix naming one site leaves the ssh fetcher on the narrow call |
| docs/design.md:1845 | "`internal/agents/agents.go:94`" | `internal/agents/agents.go:105-106` | verified by hand today: the branch `if s.ID == "" && len(s.SessionID) >= 8 {` is at :105, the assignment at :106; :94 is a `json.Unmarshal` error return |
| docs/design.md:2328 | "`internal/agents/agents.go:94-95` back-fills `ID` from `SessionID[:8]`" | **CLOSED by the §20 merge** — agents.go:94-95 citation no longer exists at that line: the two copies of `### Making the pane the row is missing` were merged into one seven-item subsection, the two `Add` directives inside them deleted as already-applied, and the three stale citations corrected in the same edit. Nothing to apply. | as above 
| docs/design.md:2330-2331 (the phrase spans the wrap: "lands in" is on :2330, "`Command` (`registry.go:189`)" on :2331 — the earlier list cited :2331 alone) | "Claude's own kind is assigned to / `Command` (`registry.go:189`)" | **CLOSED by the §20 merge** — registry.go:189 citation no longer exists at that line: the two copies of `### Making the pane the row is missing` were merged into one seven-item subsection, the two `Add` directives inside them deleted as already-applied, and the three stale citations corrected in the same edit. Nothing to apply. | verified: `p.Command = s.Kind` is at :270 
| docs/design.md:2599 | "lands in `Command` (`internal/registry/registry.go:189`) and the agent row's format drops" | `internal/registry/registry.go:270` | as above |
| docs/design.md:1873 | "the file's `pid` is null in 25 of 25 files including" | "`pid` is **ABSENT** from all 25 local `state.json` files — not null" | §22.5. An absent key is a different parse from a null one, and the ruling is unchanged and stronger |
| docs/design.md:2038, :2109, :2230, :2253 | the same "null in 25 of 25" claim, four more sites | the same ABSENT correction, and for the liveness ruling add: the LISTING's own `pid` is non-null on only 3 of 25 background rows and on none of the 7 it calls `working` or `blocked` | §22.5 |
| docs/design.md:1890 (the SIXTH site, missing from the earlier list, which said "five sites, one measurement") | "`state.json`'s own `pid` is null in **25 of 25** files, including two live `working` sessions" | the same ABSENT correction | §22.5. Five of six sites were corrected and this one was cited only for the table splice below, so the refuted word survived in §17 |
| docs/design.md:1832 | "Re-measured on **2.1.232** in §22" | Delete and replace with §22's span: "2.1.233 (local) and 2.1.224 (`nuc`), the oldest in this fleet; every door and flag taken on BOTH, and the protocol observations in §22.2 and §22.4 on the local machine only, not re-measured on 2.1.224" | **That rationale was wrong, and a spike on 2026-08-16 refuted it:** 2.1.232 is installed and a live `bg-pty-host` is executing it while the CLI reports 2.1.233. The correction stands on the narrower true fact — §22's figures were taken on 2.1.233 and 2.1.224, so a sentence attributing them to 2.1.232 mis-states its own provenance — and §22's standing rule 1 now carries the sharper consequence: an attach claims a pre-warmed spare, so the version behind a background session can be older than the one you just measured. Also `cliVersion` is present on only 28 of 32 files, so a pin against the file is undefined for 4. The span replaces "an unrecorded version": §22 states the protocol provenance ONE way, in its own standing rule 2 |
| docs/design.md:2103 (first claim on the line) | "Everything in §22 was measured on **2.1.232**, so pin that alongside the file's own `cliVersion`" | the same span | as above |
| docs/design.md:1890 (the THIRD `2.1.232` site, missing from the earlier list) | "both measured later on **2.1.232** (§22)" | the same span | as above. A version present on no machine in the fleet was left standing in §17's table paragraph |
| docs/design.md:2103 (second claim on the line) | "`claude attach\|logs\|stop` are shipped verbs that appear in **no** `--help` listing" | "absent from every listing of COMMANDS, though each prints its own usage when named — the same text, byte for byte, on 2.1.233 and 2.1.224" | §22.2. The `--cloud` false-positive trap for a reader grepping the top-level help stands unchanged |
| docs/design.md:1890 | this paragraph is spliced INTO §17's four-machine table, between the `local` row at :1885 and the third row of the table at :1888, so the table is broken in the committed file | move it below the table | verified by reading :1883-1890: :1883 header, :1884 separator, :1885 `local`, :1886 blank, :1887 prose, :1888 §17's third machine, :1889 `nuc`, :1890 `eu` |
| docs/design.md:1885 | "\| local \| **2.1.227** \| 7 \| background only \| 7 \| 0 \| 0 \|" | local **2.1.233**; 7 entries → bare 13 / `--all` 31 (25 background + 6 interactive), with the interactive cells `state` 0/6 and `status` 4/6. Leave the background `state`/`status` cells UNCOUNTED and say so | §22.6, §22.8. Every `entries` count was a bare-`--json` figure, so the column is what the default filter returns, not the population — which the table must say per column. What the LOCAL background rows carry was not measured (§22.10), so no figure is written into those two cells |
| docs/design.md:1889 | "\| `nuc` \| **2.1.224** \| 10 \| background **and interactive** \| 3 \| 9 \| **9** \|" | 10 entries → bare 11 / `--all` 14 / 7 background, with background `state` 7/7 and interactive `status` 7/7 and `state` 0/7 | §22.8, measured 2026-08-15 in both directions |
| docs/design.md:1888 | §17's third machine row (the one host besides local and `nuc` that carries claude; named in the table, not repeated here) | mark it "not re-taken" | §22's standing rule 1: of the three `claude`-bearing hosts, that one was never probed, so "both versions" covers two hosts of three |
| docs/design.md:1888 | §17's fourth machine row, the one with `no version output` | leave it alone — do NOT mark it "not re-taken" | design.md:187 and :1922 say that machine has no claude installed and returned nothing for `claude --version`. A row that was never takeable is not a gap, and marking it as one invents a probe that cannot be run |
| docs/design.md:1898 | "**pass `--debug-file` on every `claude` exec.**" | "pass `--debug-file` where the verb accepts it: `logs` ACCEPTS it (writes the file even for a bogus id, 620 B local / 278 B on `nuc`), `agents` REJECTS it (rc=1 in ~165 ms, `error: unknown option '--debug-file'`, no file, 6/6 runs), and `attach` is UNTESTED" | §22.6. "every exec" is false in both directions and the untested cell is the one §22.3's payload rests on |
| docs/design.md:363-364 | "its operational rules (always `agents --json --all`; always `--debug-file` where the / verb accepts it, **which `agents` and `logs` do not**; judge those two by exit code alone)" | "…which `agents` does not; `logs` DOES, and is the one reading that can be instrumented — judge `agents` by exit code alone" | §22.6, measured on both versions. This is the site that states it as an operational RULE, and it is backwards on the half that matters; the earlier list corrected only :1898, so the refuted claim survived verbatim |
| docs/design.md:1905 | "`attach`, `logs` and `stop` are **background** verbs, so an interactive session appearing in a 2.1.224 listing…" | Keep, and strengthen the reason: the biconditional is closed both ways on both hosts — background rows carry an `id` 32 of 32, interactive rows 0 of 13 — so the sentence is about the bytes on the wire rather than about the vendor's intent | §22.8 |
| docs/design.md:2039 | "a row that is not live: `logs` answers rc=1 in <0.2 s with `job not found — it may have already exited`" | "a row the daemon no longer holds: `logs` answers rc=1 in <0.2 s with `No job matching '<id>'. Run 'claude agents' to list running sessions.`" — and nothing anywhere may MATCH on the text | measured on both versions for all three verbs: the old wording no longer exists. This was the only remaining site carrying it AND the only site now carrying the <0.2 s latency, and the earlier list had no row for it |
| docs/design.md:316 | "that state is **invisible**: `list` polled for 136 s while the prompt was pending reported `working/active`, an empty `needs`, and no `block`" | add :728's qualifier: "through the daemon's own `list`; whether `claude agents --json --all` reports it differently is unmeasured (§22.10)" | §22.4. The measurement came through the source §22.2 forbids the hub to use, and the call the hub polls was never checked for that state |
| docs/design.md:1580 | "measured silent (`working/active`, no `block`) for 136 s on a session that was waiting." | the same qualifier | as above |
| docs/design.md:1967 | "Write permission prompt reported `working/active`, an empty `needs` and no `block` across 136 s of" | the same qualifier | as above |
| docs/design.md:2031 | "`working/active` is also what a session pending a **Write permission prompt** reports — across 136 s of polling, with an empty `needs` and no `block` (§22.4)" | the same qualifier | as above |
| docs/design.md:2166-2167 | "a session sitting on a Write permission prompt polled `working/active` with an empty / `needs` and no `block` for **136 s**" (the sentence wraps across the two lines) | the same qualifier | as above |
| docs/design.md:728, :1580, :1967, :2031, :2037, :2167 | the `working/active` and `failed/idle` PAIR notation | write the listing word alone | §22.5: over 25 rows `tempo` is a deterministic projection of the listing state (done→idle, failed→idle, blocked→blocked, working→active), so the pair is one fact written twice and it silently mixes two sources with two freshnesses — `tempo` lives in `state.json` (32/32) and is not among the listing's nine keys |
| docs/design.md:2094 | "`i` becomes possible under a whitelist (§22.4)" | "`i` on an agent row is DEFERRED (§22.4): `reply` and `subscribe` are protocol operations with no CLI verb behind them, and §22.2 rules that the hub execs verbs" | §22.4. Read as available, it contradicts :26, :48, :1357 and :2461, which all make other rulings depend on the deferral |
| docs/design.md:1035-1040 | §7's three-outcome table, "session target (§22)" column, fully specified | mark the whole column as the shape a FUTURE `i` takes, not semantics this product has | §22.7. `delivered` needs `subscribe`'s pty bytes, which §22.2 forbids and §20 bans drawing; an unmarked column for a deferred path reads as implemented |
| docs/design.md:1039 | §7's `refused` row: "the whitelist refused before any write — `tempo != \"blocked\"`, no `block` present, empty text, or the id absent from the roster" | rewrite the predicate as "the LISTING says `blocked` AND `state.json` carries a `block`", say it admitted 1 of 6 measured blocked rows, and say the `block` must be re-read INSIDE the send | §22.4. `tempo` is not in the listing and is a projection of its state word; and a `block` read on the slow sweep passes precisely the Write-prompt state the whitelist exists to exclude |
| docs/design.md:2713-2755 | "A blocked, pane-less agent row can be **shown**, and **opened** through §22's `claude attach`. It can" (:2754) / "never be *answered* from the hub" (:2755) — the sentence wraps, so neither line holds it whole | "It is not answered from the hub as designed, and §22.4 says what it would cost" | §22.4: `reply` reached a session with no tty, no pane and no viewer, `ok` in 13 ms — so "never" is false about the world, though true about this product |
| docs/design.md:571 | "row that has no pane, which is a cost §22.9's confirmation has to name alongside the abandoned turn." | rewrite as open: the host cost is §22.9's decision 2, recommended yes — or take the decision and keep the line | §22.9. A design citing its own open question as a ruling is how §17's open-question-1 strike-through happened |
| docs/design.md:2214 | "that would stop that session is `claude stop <id>`, which §22.9 leaves unoffered." | rewrite as open: whether `stop` is offered is §22.9's decision 3, recommended no | as above |
| docs/design.md:2601-2645 | "the header hint already changes on this row and the refusal already names both the cause and the remedy … a marker's worth of information in the two places the operator is already reading" | delete the header-hint half: `hintFor(pathRefuse)` falls to `default` (`internal/ui/possession.go:285-299`), so outside tmux the hint is "" for every row and inside tmux an agent row gets the same ambient nested-session hint a pane row gets | §22.8. One of the "two places" carries no information about the refusal at all; the tile half survives, weaker, since the tile is the focused row only |
| docs/design.md:2093 | "So `a` on a background agent row becomes a fourth §20 dispatch path (§22.3)" | keep the routing and settle the ordinal against §20's own copy (below) | verified: :2093 reads "a fourth §20 dispatch path" |
| docs/design.md:2274-2284 | **TWO copies of one insertion**, both opening "five honest paths … reached by seven routings" (:2277-2278 and :2282-2283). The ordinal is not inconsistent within a paragraph — there are two paragraphs: the first calls the create "The fifth" on **:2278** (the earlier list said :2277, which ends the enumeration), and the second, at :2282-2283, gives it no ordinal at all — it is merely fourth in the list's order | delete one copy (keep the one carrying the reasoning sentence), fix the ordinal once, and add a row for the background-agent create to the dispatch table beneath — its first row is "an agent row that is **not** background", so the routing §22.3 defines appears nowhere in it | verified by reading :2274-2288 |
| docs/design.md:2296 and :2353 | `### Making the pane the row is missing` appears TWICE, with different measurements (3.2a+3.7b against 3.7b only) and different counts — "Six things are load-bearing" at :2306 against "Five things are load-bearing" at :2366 | **CLOSED by the §20 merge** — the duplicated subsection no longer exists at that line: the two copies of `### Making the pane the row is missing` were merged into one seven-item subsection, the two `Add` directives inside them deleted as already-applied, and the three stale citations corrected in the same edit. Nothing to apply. | verified by reading both headings and both count sentences 
| docs/design.md:2267, :2424, :2453, :2589, :2629, :2653, :2661 | **seven** paragraphs beginning `Add:` or `Add one clause:` — unapplied edit directives sitting verbatim in §20's prose | **CLOSED by the structural commit** — the directive was spliced mid-sentence into §20's opening and its content is refuted by §22.4, so it was deleted rather than applied. Nothing to apply. | verified by reading each line; `grep -c '^Add:\|^Add one clause:'` returns 7, not the eight the earlier list claimed. None is marked TODO anywhere 
| docs/design.md:2415 | `Add after "…falls back to the full-screen attach.": "The fifth routing is not a property of running inside tmux…"` — an eighth unapplied directive in a DIFFERENT form, which a rule matching `Add:`/`Add one clause:` would not catch | **CLOSED by the §20 merge** — the spliced Add directive no longer exists at that line: the two copies of `### Making the pane the row is missing` were merged into one seven-item subsection, the two `Add` directives inside them deleted as already-applied, and the three stale citations corrected in the same edit. Nothing to apply. | verified by reading the line. Split out because the earlier list counted it among the seven and stated a prefix rule that does not match it 
| docs/known-issues.md:175 | N4's "**Residue:** two sessions sharing an id AND a cwd would still merge, which does not occur on this fleet and cannot be helped by the name" | "Residue: two sessions sharing an id AND a cwd still merge, and under the `--all` call §22.6 mandates it DOES occur — 31 rows / 30 distinct keys / 1 row silently lost, the pair differing only in `Kind`. Fixed by putting `Kind` in `agentRowID` (§22.11)." Also correct N4's own `agents.go:94-96` and `registry.go:180`/`:187-194` citations to `:105-106` and `registry.go:196-198`/`:261-282` | §22.11. A comment asserting that a collision does not occur is worse than no comment: it is what stops the next reader checking. `internal/registry/registry.go:254` carries the identical sentence and must change in the same commit |
| docs/known-issues.md:177 | N6's "Measured over 21 real sessions: raw words were `idle 7, blocked 5, working 3, busy 2, done 2, absent 2`" and "a project of five sessions currently reads `? 4 of 5`" | keep the 21-row census, dated, and add that the vocabulary splits by KIND on the NEWEST version: in the local `--all` call of 2026-08-15 the 6 interactive rows carried `status` 4/6 and `state` 0/6, and on `nuc` background rows carry `state` 7/7 while interactive rows carry `status` 7/7 and `state` 0/7 | §22.6, §22.8. N6's fix stands; what changes is the reason a filter over `Attention()` is unsafe — it drops live interactive rows on 2.1.233, not only on the older half of the fleet. No figure is given for the LOCAL background rows: that split was not counted (§22.10) |
| internal/agents/attention_test.go:25, :28 | "`2.1.224`'s word for the same thing — MEASURED on 2 of 21 rows" / "on 7 of 21 rows" | keep the words and re-date the counts, or drop the counts: the fixtures are per measured WORD, which is the right shape, and the row counts are a 2026-08-13 census that `--all` supersedes | §22.6. `idle` is measured on 2.1.233 interactive rows too, so attributing it to 2.1.224 alone is the misattribution §22.6 warns against |

